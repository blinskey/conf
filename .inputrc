set editing-mode vi
Control-l: clear-screen

# Starting with version 4.3 of Bash, this will cause the mode to be prepended
# to the prompt.
set show-mode-in-prompt on

# This reduces the delay when switching from insert mode to normal mode in the
# shell.
set keyseq-timeout 50
